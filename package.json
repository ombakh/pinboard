{
  "name": "social-media",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack forum monorepo (React + Node.js)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "start": "npm run start --workspace server"
  }
}
